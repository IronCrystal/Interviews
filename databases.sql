transaction (id, userId, amount, invoiceId, description)

-- Write a query that will return all transactions done by userId '10'

-- Write a query that will return the total amount each user spent

users (id, name, emailAddress)

-- Write a query that returns all transactions (id, amount, invoiceId, description) and user(emailAddress)

-- Optimize Query #1

-- Explain the difference between LEFT JOIN, INNER JOIN, and FULL/OUTER JOIN